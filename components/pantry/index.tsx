"use client";

// Pantry page component — implemented in Phase 04c
export function PantryPage() {
  return (
    <div
      style={{
        minHeight: "100dvh",
        background: "var(--bg)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "var(--fg-2)",
        fontFamily: "var(--font-sans)",
        fontSize: 16,
      }}
    >
      Pantry — coming soon
    </div>
  );
}
