// Shared style primitives for the profile settings rows, used by the page and
// by row components extracted into ./components (e.g. push-notification-toggle).

export const ROW_ICON_SIZE = 15;
export const ROW_ICON_STROKE_WIDTH = 1.75;
export const CHEVRON_ICON_SIZE = 13;
export const CHEVRON_STROKE_WIDTH = 2;

export const ROW_CLASSES =
  "flex items-center gap-2 py-3.5 px-4 bg-transparent border-none w-full text-left";

export const ROW_LABEL_CLASSES =
  "flex-1 font-sans text-sm font-medium text-[var(--fg-1)]";
export const ROW_ICON_CLASSES = "shrink-0 text-[var(--fg-2)]";
